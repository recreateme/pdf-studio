# ── Python ─────────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg
*.egg-info/
.eggs/
pip-wheel-metadata/
pip-log.txt
*.manifest
*.spec.bak

# 虚拟环境
.venv/
venv/
env/
ENV/

# ── 测试 / 覆盖率（保留 tests/ 源码，仅忽略运行产物）──
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.tox/
.nox/
.hypothesis/
*.cover
.cache/

# ── 类型检查 / 工具缓存 ───────────────────────────────
.mypy_cache/
.ruff_cache/
.pytype/
.dmypy.json

# ── 打包 / 构建产物 ───────────────────────────────────
build/
dist/
dist_release/
*.spec.bak
pack_smoke_result.txt
pack_smoke_gui_ok.txt

# PyInstaller 中间文件（若落在根目录）
warn-*.txt
xref-*.html

# Nuitka / 其他打包
*.build/
*.dist/
*.onefile-build/

# ── 运行时生成（日志、缓存、临时文件）────────────────
logs/
cache/
*.log
*.log.*

# 用户本地配置（若在项目内调试生成）
config.local.json
.env
.env.*
!.env.example

# ── IDE / 编辑器 / 本地工具 ───────────────────────────
.idea/
.vscode/
*.sublime-project
*.sublime-workspace
.cursor/

# ── 操作系统隐藏/系统文件 ─────────────────────────────
.DS_Store
.AppleDouble
.LSOverride
Thumbs.db
Thumbs.db:encryptable
ehthumbs.db
ehthumbs_vista.db
Desktop.ini
$RECYCLE.BIN/
*.lnk

# ── 杂项 ─────────────────────────────────────────────
*.tmp
*.temp
*.bak
*.swp
*~
.#*

# 误生成的乱码文档副本（根目录已有 使用说明.md）
docs/浣跨敤璇存槑.md

# Playwright 浏览器缓存（若在项目目录内安装）
ms-playwright/

# OCR 模型缓存（若在项目内下载）
.rapidocr/
